var x int32 = 10;
var y int32 = 20;

// Verificando que la comparación devuelve un booleano y respeta precedencia
var esMayor bool = x + 15 > y;     // 25 > 20 -> true
var esIgual bool = x == 10;        // true
var esDiferente bool = "hola" != "adios"; // true

// Debería tirar error semántico:
var err bool = "hola" == 10;