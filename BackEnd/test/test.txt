// Declaraciones globales
var mensaje string = "Probando JSON"
const pi float32 = 3.14159

func multiplicacion(a int32, b int32) int32 {
    var resultado int32 = a * b
    return resultado
}

func main() {
    // 1. Ejecución normal (Esto irá a la Consola y a la Tabla de Símbolos)
    var base int32 = 10
    var calculo int32 = multiplicacion(base, 5)
    
    fmt.Print("El cálculo es: ", calculo)
    
    // 2. Funciones Embebidas
    var texto string = "Golampi"
    var longitud int32 = len(texto)
    var fecha string = now()
    
    fmt.Print("Longitud de la palabra: ", longitud)
    fmt.Print("Fecha de ejecución: ", fecha)

    // 3. ERRORES FORZADOS (Esto llenará el arreglo de Errores)
    // Error 1: Variable ya declarada
    var base int32 = 20 
    
    // Error 2: Asignar un tipo incorrecto
    var numero int32 = "esto es una cadena" 
    
    // Error 3: Llamar a una función que no existe
    funcionFantasma()
}