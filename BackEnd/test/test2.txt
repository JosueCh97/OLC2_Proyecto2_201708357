// 1. Declaramos y asignamos valores primitivos
var x int32 = 10;
var y int32 = 5;

// 2. Usamos AccesoID para leer 'x' e 'y', y Aritmetico para operarlos
var z int32 = x + y * 2; 

// 3. Verificamos asignaciones múltiples operando con variables
var a, b int32 = z - 2, x % 3;